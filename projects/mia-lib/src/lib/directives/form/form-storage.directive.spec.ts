import { FormStorageDirective } from './form-storage.directive';

describe('FormStorageDirective', () => {
  it('should create an instance', () => {
    const directive = new FormStorageDirective();
    expect(directive).toBeTruthy();
  });
});
